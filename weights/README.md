# Weights

This folder can host open-source model assets that are small enough for standard Git hosting.

Recommended layout:

```text
weights/
  nrq/
    <organ>.npz
```

Notes:
- Keep individual files below GitHub's hard limit (100 MB per file).
- For larger assets, use external hosting or Git LFS.
